diff --git a/EppoScriptCore.Native/CMakeLists.txt b/EppoScriptCore.Native/CMakeLists.txt
index 74b236e..ab1c24c 100644
--- a/EppoScriptCore.Native/CMakeLists.txt
+++ b/EppoScriptCore.Native/CMakeLists.txt
@@ -17,7 +17,10 @@ set(INCLUDE_DIRS
 )
 
 target_include_directories(${PROJECT_NAME} PRIVATE ${INCLUDE_DIRS})
-target_include_directories(${PROJECT_NAME} PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/Include")
+target_include_directories(${PROJECT_NAME} PUBLIC
+    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/Include>"
+    "$<INSTALL_INTERFACE:include>"
+)
 target_compile_definitions(${PROJECT_NAME} PRIVATE $<$<CONFIG:Debug>:EP_DEBUG> $<$<CONFIG:RelWithDebInfo>:EP_RELEASE> $<$<CONFIG:Release>:EP_DIST>)
 target_precompile_headers(${PROJECT_NAME} PRIVATE "Source/pch.h")
 
@@ -27,4 +30,29 @@ endif ()
 
 if (CMAKE_SYSTEM_NAME STREQUAL "Linux")
     target_link_libraries(${PROJECT_NAME} PUBLIC ${CMAKE_DL_LIBS})
-endif ()
\ No newline at end of file
+endif ()
+
+include(CMakePackageConfigHelpers)
+
+install(TARGETS ${PROJECT_NAME}
+    EXPORT ${PROJECT_NAME}Targets
+    ARCHIVE DESTINATION lib
+)
+
+install(DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/Include/EppoScriptCore.Native"
+    DESTINATION include
+)
+
+install(DIRECTORY "${CMAKE_SOURCE_DIR}/EppoScriptCore.Managed"
+    DESTINATION share/epposcriptcore
+    FILES_MATCHING
+    PATTERN "*.cs"
+    PATTERN "*.csproj"
+    PATTERN "*.json"
+)
+
+install(EXPORT ${PROJECT_NAME}Targets
+    FILE EppoScriptCoreTargets.cmake
+    NAMESPACE EppoScriptCore::
+    DESTINATION share/epposcriptcore
+)
